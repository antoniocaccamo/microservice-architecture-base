spring:
  application:
    name : proxy-service
  cloud:
    config:
      discovery : 
        uri : true
        service-id: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}